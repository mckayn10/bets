import {StyleSheet, Text, View} from 'react-native'

const Bet_Type_Screen = props => {
    return (
        <View styles={styles.container}>
            <Text>Hello</Text>
        </View>
    )
}

const styles = StyleSheet.create({
    container: {
        flex: 1
    }

})

export default Bet_Type_Screen